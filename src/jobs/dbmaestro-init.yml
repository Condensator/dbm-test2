description: >
  Sample description
# What will this job do?
# Descriptions should be short, simple, and clear.

executor: default

parameters:
  version:
    type: string
    default: "@latest"
    description: "DBmaestro Automation Agent version"
steps:
  - restore_cache:
      keys:
        - dbmaestro-automation-jar-{{ checksum "DBmaestroAgent.jar" }}
  - download-automation-jar:
      tool_path: "~/usr/local/tools/dbmaestro"
  - save_cache:
      paths:
        - ~/usr/local/tools/dbmaestro  # default location
      key: dbmaestro-automation-jar-{{ checksum "DBmaestroAgent.jar" }}
